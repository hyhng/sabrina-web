# cms

Payload 3 admin for the client. **Not built yet — this is phase F3.**

It needs Docker and Postgres, neither of which F1 requires. The package exists
so the workspace matches the layout in `docs/TECH.md` section 3 and F3 has
somewhere to land.

See `docs/PHASES.md` → F3.

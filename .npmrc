# One copy of React/Next across the workspace, so packages/ui does not end up
# with a second React. See docs/TECH.md section 2.
link-workspace-packages=true
prefer-workspace-packages=true
engine-strict=true
